apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base

# Placeholder for future staging-specific patches or resources
# patches:
#   - path: patches/helm-release-patch.yaml
#     target:
#       kind: HelmRelease
#       name: nvidia-gpu-operator

